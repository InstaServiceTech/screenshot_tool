# Example only. Copy to .env locally, or set GitHub secrets for the host.
# Do not commit .env. Do not use local defaults on production.

SECRET_KEY=replace-with-a-long-random-string
AUTH_USERS=name1:password1,name2:password2
